#Attacking_machine 
Add hostname and domain  to `/etc/hosts`

```bash
sudo sh -c 'echo "10.129.12.60  DC01.darkzero.htb darkzero.htb" >> /etc/hosts'
```

**Next step:** [[SMB Enumeration (unauthenticated)]]
